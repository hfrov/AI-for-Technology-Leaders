# [MERGED - DELETE THIS FILE]

This chapter has been merged into **04-ai-first-engineering.md**, which is now the single canonical version (it had one paragraph more than this file - the opening of the "Team Model" section - so it was kept as the source of truth).

This file is a duplicate and should be removed from the repo:

```
git rm 03-ai-first-engineering.md
git commit -m "Remove duplicate AI-First Engineering chapter, merged into 04"
```

Claude could not delete files directly during this cleanup pass (2026-07-08), so this stub is left in place instead of silently losing content. Full original text is preserved in git history if you ever need it.
